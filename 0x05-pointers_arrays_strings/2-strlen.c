#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: The character to prints
 *
 * Return: Always 0 .
 */

int _strlen(char *s)
{
	int i;

	for (i = '0' ; S(i) != '\0'; i++)
	{
		return (i);
	}
}

