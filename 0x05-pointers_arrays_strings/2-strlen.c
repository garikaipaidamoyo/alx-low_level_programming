#include "main.h"

/**
 * _strlen - accepts a pointer to an array as argument.
 * @s: char var
 *
 * return: return i
 */

int _strlen(char *s)
{
	int i;

	i = 0;
	while (s[i] != '\0')
	
		return (i);
}
