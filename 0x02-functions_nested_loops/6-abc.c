#include "main.h"

/**
 * _abc - computes the absolute value of an integer to stdout.
 *
 *
 * returns: -1 on success
 * on failure returns 1
 */
int _abs(int)
{
	int i;

	if (i < 0)
	{
		return (-i);
	}
	else
		return (i);
}
