#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i = 'a'; n = 'z';

	while (i <= 'A'; n <= 'Z')
	{
		putchar(i);
		i++;
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);

}
